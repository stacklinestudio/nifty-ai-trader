# Data Sources

Kite Connect is the primary authorised broker/data adapter. Optional global/news/breadth adapters must label source, timestamp, and availability. Missing, delayed, malformed, or stale values are never invented and cause a no-trade outcome where required for safety.
