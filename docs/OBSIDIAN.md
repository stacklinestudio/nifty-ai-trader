# Obsidian Export

Set `OBSIDIAN_VAULT_PATH` to enable Markdown export beneath `NIFTY AI Trader/`. Exports include Daily Research, Trade Journal, Setups, Experiments, and strategy evolution notes. If the vault is unavailable, the exporter returns no path and the trading workflow continues.
