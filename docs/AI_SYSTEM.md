# AI System

`AIProvider` is an interface, with an explicit unavailable default. `AIRouter` validates structured `AIAnalysis` and caches unchanged fact sets. AI receives summarized facts only at controlled research/post-trade decision points. It never receives execution authority, arbitrary-code capability, or a path around the risk engine.
