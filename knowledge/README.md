# V2 Knowledge Base

This folder holds human-readable research exports and versioned experiment notes. Structured, append-only trade/agent memory is stored in SQLite; private database files are ignored. No production parameter is changed automatically.
